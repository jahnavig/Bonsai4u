<% unless cart.line_items.empty? %>
<div class="cart_title">Your Cart</div>
<table>
	<%= render(cart.line_items) %>
	<tr>
		<td colspan="2">Total</td>
		<td class="total_cell"><%= number_to_currency(cart.total_price)  %></td>
	</tr>
</table>
<%= button_to 'Empty Cart',cart, :method => :delete, :confirm => 'Are you sure?',:class=>"btn btn-success"  %>
<p>
	<%= link_to "Continue Shopping",:controller=>"bonsai_gallery",:action=>"index" %> | 
	<% unless session[:user].nil? %>
<%= link_to "Checkout" ,:controller=>"bonsai_gallery",:action=>"checkout",:amount=>cart.total_price %>
	<% else %>
	<%= link_to "Checkout", :controller=>"account",:action=>"login" %>
	<% end %>
</p>
<% end %>